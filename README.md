A simple working example using [Critical](https://github.com/addyosmani/critical)

1. Place the HTML code in `test/index.html`
2. Place the CSS code in `css/styles.css`

Run `gulp build`.

The result is `critical.css`.